# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Diagnostic reports (https://nodejs.org/api/report.html)
.node_reports/
# V8 binary code cache
.node_v8_instance_types/

# Runtime data
pids
*.pid
*.seed
*.info

# Directory for instrumented libs generated by jscoverage/JSDoc
lib-cov

# Coverage directory used by tools like Istanbul
coverage

# nyc test coverage
.nyc_output

# Grunt intermediate storage (https://gruntjs.com/configuring-tasks#tmp-files)
.grunt

# Bower dependency directory (https://bower.io/)
bower_components

# node-waf configuration
.lock-wscript

# Compiled JavaScript files
*.js
*.jsx
# For a TypeScript project, you typically commit the .ts files, not the compiled .js.
# However, if you are building a library that outputs to a 'dist' folder, you often
# ignore the entire 'dist' folder and commit the source (.ts) files.
# If your setup compiles .js next to .ts, ensure you ignore the .js.
# We will ignore the 'dist' folder as a whole later.

# TypeScript build output
# node_modules is specifically for compiled JS, so if you're building a library,
# you typically ignore the entire output directory.
# For now, let's include the common ones.

# Dependencies
node_modules
/jspm_packages

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Optional Stylelint cache
.stylelintcache

# Microbundle cache
.rpt2_cache/

# Optional REPL history
.node_repl_history

# Yarn Integrity file
.yarn-integrity

# dotenv environment variables file
.env
.env.test
.env.production
.env.development
.env*.local

# parcel-bundler cache (https://parceljs.org/.cache)
.parcel-cache

# Next.js build output
.next/
# Nuxt.js build output
.nuxt/

# Deno files
.deno/

# Output directory for TypeScript builds (e.g., where your compiled JS goes)
# This is critical for an SDK project. We'll likely build to `dist/` or `lib/`.
dist/
/lib/
/build/

# Test coverage
/.nyc_output
/coverage

# VS Code
.vscode/

# macOS specific
.DS_Store

# Windows specific
Thumbs.db
